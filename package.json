{
  "name": "manga-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "vercel-build": "next build",
    "dev:api": "turbo run dev:api",
    "dev:web": "turbo run dev:web"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "turbo": "^2.0.6",
    "typescript": "^5.5.4"
  }
}